import { Component } from '@angular/core';

// Stub route target — replaced by a later task.
@Component({
  selector: 'app-about',
  templateUrl: './about.component.html',
})
export class AboutComponent {}
