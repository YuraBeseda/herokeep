node_modules/
dist/
coverage/
# Copied by apps/web/tools/copy-packs.ts (prebuild/pretest/prestart) from packages/content's
# build output, so the "public" angular.json asset glob can serve it under /packs — see that
# script's doc comment for why it isn't a direct angular.json asset entry instead.
apps/web/public/packs/
# Playwright e2e run artifacts (apps/web/playwright.config.ts).
apps/web/test-results/
apps/web/playwright-report/
apps/web/blob-report/
*.tsbuildinfo
.angular/
.DS_Store
Thumbs.db
.env
.env.*
!.env.example
# apps/api Node adapter's local SQLite data dir (src/adapters/node/server.ts's default `HK_DATA_DIR`).
apps/api/data/
*.sqlite
*.sqlite-journal
*.sqlite-wal
*.sqlite-shm
